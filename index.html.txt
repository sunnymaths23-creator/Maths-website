<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sunny Maths Academy</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --sky1:#00c6ff;
      --sky2:#0072ff;
      --white:#ffffff;
      --dark:#0b1b3a;
      --glass: rgba(255,255,255,0.16);
      --glass2: rgba(255,255,255,0.10);
      --border: rgba(255,255,255,0.25);
      --shadow: 0 14px 40px rgba(0,0,0,0.18);
    }

    *{ box-sizing:border-box; margin:0; padding:0; }

    body{
      font-family: "Poppins", sans-serif;
      background: radial-gradient(1200px 700px at 15% 10%, rgba(255,255,255,0.35), transparent 50%),
                  radial-gradient(1200px 700px at 90% 20%, rgba(255,255,255,0.25), transparent 55%),
                  linear-gradient(135deg, var(--sky1), var(--sky2));
      color: var(--white);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Floating Maths Symbols Background */
    .floating{
      position: fixed;
      inset: 0;
      pointer-events: none;
      opacity: 0.25;
      z-index: 0;
    }
    .symbol{
      position: absolute;
      font-weight: 700;
      color: rgba(255,255,255,0.9);
      animation: floatUp 10s linear infinite;
      filter: blur(0.2px);
      user-select: none;
    }
    @keyframes floatUp{
      0% { transform: translateY(110vh) rotate(0deg); opacity: 0; }
      15% { opacity: 0.9; }
      100% { transform: translateY(-20vh) rotate(220deg); opacity: 0; }
    }

    /* Navbar */
    .navbar{
      position: sticky;
      top: 0;
      z-index: 50;
      backdrop-filter: blur(14px);
      background: rgba(0,0,0,0.12);
      border-bottom: 1px solid rgba(255,255,255,0.15);
    }
    .nav-inner{
      max-width: 1200px;
      margin: auto;
      padding: 14px 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }

    .brand{
      display:flex;
      align-items:center;
      gap:10px;
      font-weight:700;
      letter-spacing: 0.5px;
      white-space: nowrap;
    }
    .logo{
      width: 40px;
      height: 40px;
      border-radius: 12px;
      background: rgba(255,255,255,0.18);
      border: 1px solid rgba(255,255,255,0.25);
      display:grid;
      place-items:center;
      box-shadow: 0 10px 22px rgba(0,0,0,0.15);
    }

    .nav-links{
      display:flex;
      gap: 16px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }
    .nav-links a{
      color: rgba(255,255,255,0.92);
      text-decoration: none;
      font-weight: 500;
      padding: 8px 12px;
      border-radius: 999px;
      transition: 0.25s ease;
    }
    .nav-links a:hover{
      background: rgba(255,255,255,0.18);
      transform: translateY(-2px);
    }

    /* Hero */
    .hero{
      position: relative;
      z-index: 2;
      max-width: 1200px;
      margin: 28px auto 0;
      padding: 26px 18px 10px;
    }

    .hero-grid{
      display:grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 22px;
      align-items: center;
    }

    .hero-card{
      background: var(--glass);
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
      border-radius: 22px;
      padding: 26px;
      overflow:hidden;
      position: relative;
    }

    .badge{
      display:inline-flex;
      gap: 8px;
      align-items:center;
      font-size: 13px;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255,255,255,0.16);
      border: 1px solid rgba(255,255,255,0.24);
      margin-bottom: 12px;
    }

    .hero-title{
      font-size: clamp(26px, 4vw, 44px);
      line-height: 1.15;
      margin-bottom: 10px;
      font-weight: 700;
    }

    .hero-sub{
      color: rgba(255,255,255,0.92);
      font-size: 16px;
      line-height: 1.55;
      margin-bottom: 18px;
    }

    .cta-row{
      display:flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 12px;
    }

    .btn{
      border: none;
      cursor: pointer;
      padding: 12px 16px;
      border-radius: 14px;
      font-weight: 600;
      transition: 0.25s ease;
      display:inline-flex;
      align-items:center;
      gap: 10px;
      text-decoration: none;
    }
    .btn-primary{
      background: linear-gradient(135deg, rgba(255,255,255,0.95), rgba(255,255,255,0.70));
      color: #0b1b3a;
      box-shadow: 0 14px 28px rgba(0,0,0,0.2);
    }
    .btn-primary:hover{
      transform: translateY(-2px) scale(1.02);
    }
    .btn-glass{
      background: rgba(255,255,255,0.14);
      border: 1px solid rgba(255,255,255,0.28);
      color: white;
    }
    .btn-glass:hover{
      transform: translateY(-2px);
      background: rgba(255,255,255,0.20);
    }

    /* Small Info Panel */
    .hero-side{
      background: rgba(255,255,255,0.12);
      border: 1px solid rgba(255,255,255,0.22);
      border-radius: 22px;
      padding: 20px;
      box-shadow: 0 12px 26px rgba(0,0,0,0.16);
      animation: popIn 0.8s ease forwards;
      transform: translateY(10px);
      opacity: 0;
    }

    @keyframes popIn{
      to { transform: translateY(0); opacity: 1; }
    }

    .stat{
      display:flex;
      align-items:center;
      justify-content: space-between;
      gap: 10px;
      padding: 12px 12px;
      border-radius: 16px;
      background: rgba(0,0,0,0.12);
      border: 1px solid rgba(255,255,255,0.16);
      margin-bottom: 10px;
    }
    .stat strong{
      font-size: 18px;
    }
    .stat span{
      font-size: 13px;
      opacity: 0.9;
    }

    /* Sections */
    .section{
      max-width: 1200px;
      margin: 18px auto;
      padding: 16px 18px;
      position: relative;
      z-index: 2;
    }
    .section-title{
      font-size: 22px;
      margin-bottom: 8px;
      font-weight: 700;
    }
    .section-desc{
      opacity: 0.92;
      margin-bottom: 14px;
      line-height: 1.5;
    }

    .cards{
      display:grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 14px;
    }

    .card{
      background: var(--glass2);
      border: 1px solid rgba(255,255,255,0.22);
      border-radius: 20px;
      padding: 18px;
      box-shadow: 0 14px 36px rgba(0,0,0,0.15);
      transition: 0.25s ease;
      position: relative;
      overflow: hidden;
    }

    .card:hover{
      transform: translateY(-6px);
      background: rgba(255,255,255,0.16);
    }

    .card h3{
      font-size: 18px;
      margin-bottom: 6px;
      font-weight: 700;
    }

    .card p{
      font-size: 14px;
      line-height: 1.55;
      opacity: 0.92;
    }

    .mini-btn{
      margin-top: 12px;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      padding: 10px 14px;
      border-radius: 14px;
      font-weight: 600;
      border: 1px solid rgba(255,255,255,0.28);
      background: rgba(0,0,0,0.10);
      color: white;
      cursor: pointer;
      transition: 0.25s ease;
      text-decoration: none;
    }

    .mini-btn:hover{
      transform: translateY(-2px);
      background: rgba(255,255,255,0.18);
    }

    /* Footer */
    footer{
      position: relative;
      z-index: 2;
      margin-top: 30px;
      padding: 26px 18px 40px;
      text-align: center;
      opacity: 0.95;
    }
    footer p{
      opacity: 0.9;
      font-size: 14px;
      line-height: 1.6;
    }

    /* Scroll Animation */
    .fade-up{
      opacity: 0;
      transform: translateY(18px);
      transition: 0.7s ease;
    }
    .fade-up.show{
      opacity: 1;
      transform: translateY(0);
    }

    /* Responsive */
    @media (max-width: 900px){
      .hero-grid{ grid-template-columns: 1fr; }
      .nav-inner{ flex-direction: column; align-items:flex-start; }
      .nav-links{ justify-content:flex-start; }
    }

    /* Small highlight bubble */
    .glow{
      position:absolute;
      width: 380px;
      height: 380px;
      border-radius: 50%;
      background: radial-gradient(circle at 30% 30%, rgba(255,255,255,0.6), rgba(255,255,255,0) 60%);
      top: -140px;
      right: -160px;
      opacity: 0.7;
      filter: blur(1px);
      pointer-events: none;
    }
  </style>
</head>

<body>

  <!-- Floating symbols background -->
  <div class="floating" aria-hidden="true" id="floatingArea"></div>

  <!-- Navbar -->
  <div class="navbar">
    <div class="nav-inner">
      <div class="brand">
        <div class="logo">∑</div>
        <div>Sunny Maths Academy</div>
      </div>

      <div class="nav-links">
        <a href="#notes">Notes</a>
        <a href="#lectures">Lectures</a>
        <a href="#practice">Practice</a>
        <a href="#quiz">Quiz</a>
        <a href="#contact">Contact</a>
      </div>
    </div>
  </div>

  <!-- HERO -->
  <section class="hero" id="top">
    <div class="hero-grid">

      <div class="hero-card">
        <div class="glow"></div>

        <div class="badge">✨ Free Learning Platform • Class 11 • 12 • UG</div>
        <h1 class="hero-title">Fall in love with Maths 💙</h1>
        <p class="hero-sub">
          Welcome to <b>Sunny Maths Academy</b> — a vibrant place where you get
          <b>notes, video lectures, practice questions, quizzes</b> and more.
          Learn step-by-step, improve your concepts, and become confident in Mathematics.
        </p>

        <div class="cta-row">
          <a class="btn btn-primary" href="#notes">📘 Start with Notes</a>
          <a class="btn btn-glass" href="#lectures">🎥 Watch Lectures</a>
          <a class="btn btn-glass" href="#quiz">📝 Take Quiz</a>
        </div>
      </div>

      <div class="hero-side">
        <div class="stat">
          <div>
            <strong>Class 11</strong><br>
            <span>Foundation + Concepts</span>
          </div>
          <div>✅</div>
        </div>

        <div class="stat">
          <div>
            <strong>Class 12</strong><br>
            <span>Board + Competitive</span>
          </div>
          <div>✅</div>
        </div>

        <div class="stat">
          <div>
            <strong>UG Maths</strong><br>
            <span>Real Analysis + More</span>
          </div>
          <div>✅</div>
        </div>

        <div class="stat">
          <div>
            <strong>Daily Practice</strong><br>
            <span>Strong problem solving</span>
          </div>
          <div>🔥</div>
        </div>
      </div>

    </div>
  </section>

  <!-- NOTES -->
  <section class="section fade-up" id="notes">
    <h2 class="section-title">📘 Notes (PDF)</h2>
    <p class="section-desc">Download chapter-wise notes and important formulas (PDF). I will keep updating regularly.</p>

    <div class="cards">
      <div class="card">
        <h3>Class 11 • Trigonometry</h3>
        <p>Basic identities, graphs, transformations and important formulas.</p>
        <a class="mini-btn" href="#" onclick="alert('Upload your PDF file in GitHub and link it here!')">Download PDF</a>
      </div>

      <div class="card">
        <h3>Class 12 • Integration</h3>
        <p>Standard results, methods, substitution, by parts and practice set.</p>
        <a class="mini-btn" href="#" onclick="alert('Upload your PDF file in GitHub and link it here!')">Download PDF</a>
      </div>

      <div class="card">
        <h3>UG • Real Analysis</h3>
        <p>Limits, continuity, differentiability, Riemann integrals and more.</p>
        <a class="mini-btn" href="#" onclick="alert('Upload your PDF file in GitHub and link it here!')">Download PDF</a>
      </div>
    </div>
  </section>

  <!-- LECTURES -->
  <section class="section fade-up" id="lectures">
    <h2 class="section-title">🎥 Video Lectures</h2>
    <p class="section-desc">Watch lecture videos directly here. Replace the YouTube Video ID with your own lecture.</p>

    <div class="cards">
      <div class="card">
        <h3>Sample Lecture</h3>
        <p>Embed your YouTube lecture here for students.</p>

        <div style="margin-top:12px; border-radius:18px; overflow:hidden; border:1px solid rgba(255,255,255,0.18);">
          <iframe
            width="100%"
            height="290"
            src="https://www.youtube.com/embed/VIDEO_ID_HERE"
            title="YouTube video"
            allowfullscreen
            style="border:none;">
          </iframe>
        </div>

        <p style="margin-top:12px; font-size:13px; opacity:0.9;">
          ✅ Replace <b>VIDEO_ID_HERE</b> with your YouTube video ID.
        </p>
      </div>

      <div class="card">
        <h3>Quick Tip</h3>
        <p>
          You can record Maths lectures with XP-Pen tablet + OBS Studio and upload on YouTube.
          Then embed the video here.
        </p>
        <a class="mini-btn" href="#contact">📩 Ask Doubt</a>
      </div>
    </div>
  </section>

  <!-- PRACTICE -->
  <section class="section fade-up" id="practice">
    <h2 class="section-title">🧠 Practice Zone</h2>
    <p class="section-desc">Daily practice is the secret of mastering Maths. Solve questions and build speed.</p>

    <div class="cards">
      <div class="card">
        <h3>Daily Practice (10 Questions)</h3>
        <p>Simple-to-advanced questions for consistency and confidence.</p>
        <a class="mini-btn" href="#" onclick="alert('We can add daily question sets here!')">Start Practice</a>
      </div>

      <div class="card">
        <h3>Assignments</h3>
        <p>Download assignment sheets and revise important topics.</p>
        <a class="mini-btn" href="#" onclick="alert('Upload assignment PDFs and link here!')">Get Assignment</a>
      </div>

      <div class="card">
        <h3>Formula Sheet</h3>
        <p>Fast revision sheet to boost marks and confidence.</p>
        <a class="mini-btn" href="#" onclick="alert('Upload formula PDF and link here!')">Download</a>
      </div>
    </div>
  </section>

  <!-- QUIZ -->
  <section class="section fade-up" id="quiz">
    <h2 class="section-title">📝 Quiz & Tests</h2>
    <p class="section-desc">Weekly quizzes + chapter tests. You can connect Google Forms or Quizizz.</p>

    <div class="cards">
      <div class="card">
        <h3>Weekly Quiz (Google Form)</h3>
        <p>Test your understanding and check your progress.</p>
        <a class="mini-btn" href="https://forms.google.com" target="_blank">Open Quiz</a>
      </div>

      <div class="card">
        <h3>Quizizz / Kahoot</h3>
        <p>Fun and interactive MCQs to learn quickly.</p>
        <a class="mini-btn" href="https://quizizz.com" target="_blank">Start Quizizz</a>
      </div>

      <div class="card">
        <h3>Board Exam Practice</h3>
        <p>Topic-wise MCQs + important questions for Class 12.</p>
        <a class="mini-btn" href="#" onclick="alert('I can add board style tests here!')">Start</a>
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section class="section fade-up" id="contact">
    <h2 class="section-title">📩 Contact</h2>
    <p class="section-desc">Need help or want to suggest a topic? Contact me anytime.</p>

    <div class="cards">
      <div class="card">
        <h3>Teacher</h3>
        <p><b>Sunny Kumar</b> <br> Maths Teacher • Notes • Lectures • Guidance</p>
        <a class="mini-btn" href="#" onclick="alert('Add your email / WhatsApp link here!')">Message Me</a>
      </div>

      <div class="card">
        <h3>YouTube Channel</h3>
        <p>Watch all lectures on YouTube (add your channel link here).</p>
        <a class="mini-btn" href="#" onclick="alert('Paste your YouTube channel link in this button!')">Open Channel</a>
      </div>

      <div class="card">
        <h3>Motivation 💙</h3>
        <p>
          “Mathematics is not about numbers, it’s about understanding.”
          <br><br>
          Keep practicing and you will master it ✨
        </p>
        <a class="mini-btn" href="#top">⬆ Back to Top</a>
      </div>
    </div>
  </section>

  <footer>
    <p>
      © 2026 <b>Sunny Maths Academy</b> • Made with 💙 using GitHub Pages (100% Free)<br>
      Designed to make Maths easy, vibrant and enjoyable.
    </p>
  </footer>

  <script>
    // Floating symbols generator
    const floatingArea = document.getElementById("floatingArea");
    const symbols = ["π","√","∑","∞","∆","θ","+","−","×","÷","∫","≈","≠","≤","≥"];
    const total = 26;

    for(let i=0; i<total; i++){
      const el = document.createElement("div");
      el.className = "symbol";
      el.textContent = symbols[Math.floor(Math.random()*symbols.length)];
      el.style.left = Math.random()*100 + "%";
      el.style.fontSize = (14 + Math.random()*28) + "px";
      el.style.animationDuration = (8 + Math.random()*10) + "s";
      el.style.animationDelay = (Math.random()*6) + "s";
      floatingArea.appendChild(el);
    }

    // Fade-up animation on scroll
    const fadeElements = document.querySelectorAll(".fade-up");
    const observer = new IntersectionObserver((entries)=>{
      entries.forEach(entry=>{
        if(entry.isIntersecting){
          entry.target.classList.add("show");
        }
      });
    }, { threshold: 0.12 });

    fadeElements.forEach(el=>observer.observe(el));

    // Smooth scroll
    document.querySelectorAll('a[href^="#"]').forEach(anchor=>{
      anchor.addEventListener("click", function(e){
        const targetId = this.getAttribute("href");
        if(targetId.length > 1){
          e.preventDefault();
          document.querySelector(targetId).scrollIntoView({behavior:"smooth"});
        }
      });
    });
  </script>

</body>
</html>
